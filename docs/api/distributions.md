# Distributions

Prior distributions used in the `unknown_parameters_prior` dict. Each exposes
`evaluate(x)`, `cdf(x)` and `sample(min, max, seed)`.

## Normal

::: py_replay_bg.distributions.Normal

## LogNormal

::: py_replay_bg.distributions.LogNormal

## Gamma

::: py_replay_bg.distributions.Gamma

## Uniform

::: py_replay_bg.distributions.Uniform
