[project]
name = "replaybg"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "numba",
    "numpy",
    "pandas",
    "scipy",
    "pyarrow",
    "matplotlib>=3.10.8",
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]",
]
